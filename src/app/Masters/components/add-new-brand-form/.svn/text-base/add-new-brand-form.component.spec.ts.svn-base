import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { AddNewBrandFormComponent } from './add-new-brand-form.component';

describe('AddNewBrandFormComponent', () => {
  let component: AddNewBrandFormComponent;
  let fixture: ComponentFixture<AddNewBrandFormComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ AddNewBrandFormComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(AddNewBrandFormComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
