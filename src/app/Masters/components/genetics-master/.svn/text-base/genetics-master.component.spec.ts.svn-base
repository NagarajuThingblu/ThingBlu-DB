import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { GeneticsMasterComponent } from './genetics-master.component';

describe('GeneticsMasterComponent', () => {
  let component: GeneticsMasterComponent;
  let fixture: ComponentFixture<GeneticsMasterComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ GeneticsMasterComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(GeneticsMasterComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
