import { OilMaterialsInComponent } from './oil-materials-in.component';
import { async, ComponentFixture, TestBed } from '@angular/core/testing';



describe('OilMaterialsIn', () => {
  let component: OilMaterialsInComponent;
  let fixture: ComponentFixture<OilMaterialsInComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ OilMaterialsInComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(OilMaterialsInComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
