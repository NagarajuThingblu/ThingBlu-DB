import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { SiftingComponent } from './sifting.component';

describe('SiftingComponent', () => {
  let component: SiftingComponent;
  let fixture: ComponentFixture<SiftingComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ SiftingComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(SiftingComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
